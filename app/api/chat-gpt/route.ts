// app/api/chat-gpt/route.ts
import { NextRequest, NextResponse } from "next/server";

export async function POST(req: NextRequest) {
  try {
    const { message } = await req.json();

    if (!message || typeof message !== "string") {
      return NextResponse.json(
        { error: "message is required" },
        { status: 400 }
      );
    }

    const apiKey = process.env.OPENAI_API_KEY;
    if (!apiKey) {
      console.error("❌ Нет OPENAI_API_KEY в .env.local");
      return NextResponse.json(
        { error: "Server API key is not configured" },
        { status: 500 }
      );
    }

    const apiRes = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model: "gpt-4o-mini", // можешь заменить на другой
        messages: [
  {
    role: "system",
    content:  `
Ты — умный ИИ-ассистент на сайте INVISIOO, который помогает людям с разными видами инвалидности.
Отвечай дружелюбно, просто, понятно и коротко.

Твои задачи:

1) Давать советы по доступности мест (коляска, слабовидящие, слабослышащие, ОДА, временная травма, интеллектуальные особенности).
2) Объяснять условия доступности объектов: пандусы, лифты, навигация, туалеты, ширина проходов, тактильные полосы и т.п.
3) Помогать пользователю подобрать подходящее место, основываясь на его потребностях.
4) Давать рекомендации по улучшению доступности — только безопасные и реальные советы.
5) Если пользователь спрашивает про маршрут — давай текстовые подсказки, как лучше добраться, без опасных советов.

6) Если пользователь спрашивает про работу, вакансии, поиск работы, резюме, профессии или навыки:
   - ВСЕГДА говори, что на сайте INVISIOO есть раздел «Вакансии» на странице /vacancies.
   - Никогда не пиши, что у тебя нет доступа к интернету или к сайту. Просто утверждай, что раздел «Вакансии» существует на сайте.
   - Не придумывай конкретные открытые вакансии, которых нет.
   - Помогай с выбором направления, подсказками по навыкам, сильным сторонам, портфолио и тексту отклика/резюме.
   - В конце такого ответа ОБЯЗАТЕЛЬНО добавляй фразу в духе:
     «Актуальные предложения вы можете посмотреть на сайте Invisioo в разделе “Вакансии” и выбрать то, что вам подходит».

7) Если пользователь спрашивает что-то общее (не про карту/доступность/работу) — отвечай как обычный помощник, но кратко и по делу.

Правила общения:
- Пиши коротко и полезно.
- Не используй сложные термины.
- Всегда учитывай тип инвалидности пользователя, если он его указал.
- Не выдумывай факты.
- Если информации мало — можешь уточнить один-два вопроса.
- НИКОГДА не говори, что у тебя нет доступа к интернету или к сайту. Отвечай так, будто ты знаешь структуру сайта INVISIOO и раздел «Вакансии» там точно есть.

Ты — дружественный помощник, цель которого — помочь пользователю в рамках сервиса INVISIOO.

`,
  },
  {
    role: "user",
    content: message,
  },
],

        temperature: 0.4,
      }),
    });

    if (!apiRes.ok) {
      const errText = await apiRes.text();
      console.error("OpenAI API error:", errText);
      return NextResponse.json(
        { error: "Failed to call OpenAI" },
        { status: 500 }
      );
    }

    const data = await apiRes.json();

    const answer =
      data.choices?.[0]?.message?.content ??
      "Я затрудняюсь ответить, попробуйте переформулировать вопрос 🙏";

    return NextResponse.json({ answer });
  } catch (e) {
    console.error("GPT route error:", e);
    return NextResponse.json(
      { error: "Internal error" },
      { status: 500 }
    );
  }
}
